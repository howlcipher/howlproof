{
  "summary": "Re-read the file and match the same pattern.",
  "requires_service": false,
  "notes": "",
  "steps": [
    {
      "description": "Search tests/fixtures/vulnerable_app/src/config.py for the github_token pattern.",
      "kind": "file_probe",
      "payload": {
        "path": "tests/fixtures/vulnerable_app/src/config.py",
        "pattern": "gh[pousr]_[A-Za-z0-9]{30,}"
      },
      "expect": {
        "matches": true
      }
    }
  ]
}
