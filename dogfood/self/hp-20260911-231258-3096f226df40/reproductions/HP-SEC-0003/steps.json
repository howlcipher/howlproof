{
  "summary": "Re-read the file and match the same pattern.",
  "requires_service": false,
  "notes": "",
  "steps": [
    {
      "description": "Search tests/test_contracts.py for the assigned_credential pattern.",
      "kind": "file_probe",
      "payload": {
        "path": "tests/test_contracts.py",
        "pattern": "(?i)\\b(?:password|passwd|api[_-]?key|secret[_-]?key|access[_-]?token)\\b\\s*[:=]\\s*[\\\"']([^\\\"'\\s]{8,})[\\\"']"
      },
      "expect": {
        "matches": true
      }
    }
  ]
}
